/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['sagspot.s3.eu-central-1.amazonaws.com'],
  },
};

module.exports = nextConfig;
